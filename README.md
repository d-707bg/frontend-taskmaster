# TaskMaster Frontend

TaskMaster is a task management platform created primarily as a learning project for studying Go (Golang) and building a full-stack application with a modern frontend and a backend API.

## 🚀 Features
- Task creation: Users can create new tasks with a title and description.
- Task deletion: Users can delete existing tasks.
- Task updating: Users can update the title and description of existing tasks.
- Task fetching: The application fetches tasks from a backend server when it mounts.
- Filtering and sorting: The application allows users to filter and sort tasks based on various criteria.

## 🛠️ Tech Stack
- Frontend: React, React DOM, and Tailwind CSS for styling.
- Backend: **[First-Go-App Backend](https://github.com/ikok07/cvbot-backend.git)**
- Build tool: Vite
- Dependencies: Various libraries and frameworks, including `lucide-react` for icons.
- Development server: Vite development server.

## 📦 Installation
To install the application, follow these steps:
1. Clone the repository using `git clone`.
2. Navigate to the project directory using `cd`.
3. Install the dependencies using `npm install` or `yarn install`.
4. Start the development server using `npm run dev` or `yarn dev`.

## 💻 Usage
To use the application, follow these steps:
1. Start the development server using `npm run dev` or `yarn dev`.
2. Open a web browser and navigate to `http://localhost:3000`.
3. Create, delete, and update tasks as needed.

## 📂 Project Structure
```markdown
.
├── node_modules
├── public
├── src
│   ├── App.tsx
│   ├── main.tsx
│   └── ...
├── vite.config.ts
├── package.json
└── ...
```

This frontend is designed to work with the **[First-Go-App Backend](https://github.com/ikok07/cvbot-backend.git)**.
